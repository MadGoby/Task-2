mixin textfield-subscription(hint, appointment, state)
  if state !=null
    form(action="").text-field-subscription-body   
      h3.margin40.type-h3(class= state)= appointment
      div.arrow-block
        input.type-simple-text.text-field.text-field-subscription(placeholder= hint)
  else if appointment != null
    form(action="").text-field-subscription-body  
      h3.margin40.type-h3= appointment
      div.arrow-block
        input.type-simple-text.text-field.text-field-subscription(placeholder= hint)
  else
    form(action="").text-field-subscription-body
      div.arrow-block
        input.type-simple-text.text-field.text-field-subscription(placeholder= hint)