mixin login__title(settings)
  - let text = ""
  - if (settings) { 
  -   settings.hasOwnProperty("text") ? text = settings.text : text = ""
  - }
  .login__title
    +title({
      text: text,
      type: "big"
    })