mixin rate-counter(settings)
  - let value = ""
  - if (settings) {
  -   settings.hasOwnProperty("value") ? value = settings.value : value = ""
  - }
  p.text_type_simple.rate-counter 
    em.rate-counter__value=value
    =" Отзывов"
