mixin footer__logo(settings)
  - let text = ""
  - if (settings) {
  -   settings.hasOwnProperty("text") ? text = settings.text : text = ""  
  - }
  .footer__logo
    +logo({
      src: "img/logo-color.svg"
    })
    p.text_type_simple.footer__logo-description=text